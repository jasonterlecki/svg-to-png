# svg-to-png

SVG → raster conversion toolkit (CLI + library) powered by a headless browser renderer. The project is still under active development—see `AGENT.md` for the detailed product spec.

## Getting started

```bash
npm install
npm run build
```

Further documentation will be added alongside the CLI and library implementation.
